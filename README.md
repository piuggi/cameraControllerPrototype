cameraControllerPrototype
=========================

Express App Prototype to Test new gphoto2 implementation, currently only supports OSX.

Running the app
``bash
$ cd /path/to/cameraControllerPrototype
$ bin/www

``
